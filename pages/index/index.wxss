.page-container {
  background: linear-gradient(135deg, #f8f9fa 0%, #f0f5ff 100%);
  min-height: 100vh;
  padding-bottom: 20px;
}

/* Tab导航 */
.tab-nav {
  display: flex;
  background-color: #ffffff;
  border-bottom: 2px solid #e0e0e0;
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
}

.tab-item {
  flex: 1;
  text-align: center;
  padding: 14px 0;
  font-size: 15px;
  font-weight: 600;
  color: #999;
  border-bottom: 4px solid transparent;
  transition: all 0.3s ease;
}

.tab-item.active {
  color: #2DC78F;
  border-bottom-color: #2DC78F;
}

/* Tab内容 */
.tab-content {
  display: none;
  animation: fadeIn 0.3s ease;
}

.tab-content.active {
  display: block;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 页面标题 */
.page-header {
  text-align: center;
  padding: 24px 16px 16px;
}

.header-emoji {
  font-size: 48px;
  display: block;
  margin-bottom: 8px;
  animation: bounce 2s infinite;
}

@keyframes bounce {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10px);
  }
}

.header-title {
  font-size: 24px;
  font-weight: 700;
  color: #2DC78F;
  margin-bottom: 4px;
}

.header-subtitle {
  font-size: 13px;
  color: #999;
}

/* 卡片 */
.card {
  background-color: #ffffff;
  margin: 12px;
  border-radius: 16px;
  padding: 18px;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.08);
  border: 1px solid rgba(45, 199, 143, 0.1);
}

.card-title {
  display: flex;
  align-items: center;
  font-size: 16px;
  font-weight: 700;
  color: #333;
  margin-bottom: 16px;
  padding-bottom: 12px;
  border-bottom: 2px solid #f0f0f0;
}

.title-emoji {
  font-size: 20px;
  margin-right: 8px;
  display: inline-block;
}

/* 输入组 */
.input-group {
  margin-bottom: 18px;
}

.input-label {
  font-size: 13px;
  color: #666;
  margin-bottom: 10px;
  display: block;
  font-weight: 600;
}

.input-field {
  width: 100%;
  padding: 12px 14px;
  border: 2px solid #e0e0e0;
  border-radius: 10px;
  font-size: 15px;
  color: #333;
  background-color: #ffffff;
  box-sizing: border-box;
  transition: all 0.3s ease;
}

.input-field:focus {
  border-color: #2DC78F;
  box-shadow: 0 0 0 3px rgba(45, 199, 143, 0.1);
}

.input-field::placeholder {
  color: #ccc;
}

/* 性别按钮 */
.gender-buttons {
  display: flex;
  gap: 12px;
}

.gender-btn {
  flex: 1;
  padding: 12px;
  border: 2px solid #e0e0e0;
  border-radius: 10px;
  font-size: 14px;
  color: #333;
  background-color: #ffffff;
  transition: all 0.3s ease;
  font-weight: 600;
}

.gender-btn.active {
  border-color: #2DC78F;
  background: linear-gradient(135deg, #e8f5f0 0%, #f0faf8 100%);
  color: #2DC78F;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.15);
}

/* 强度按钮 */
.intensity-buttons {
  display: flex;
  gap: 12px;
}

.intensity-btn {
  flex: 1;
  padding: 14px 8px;
  border: 2px solid #e0e0e0;
  border-radius: 10px;
  font-size: 12px;
  color: #333;
  background-color: #ffffff;
  transition: all 0.3s ease;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 80px;
}

.intensity-emoji {
  font-size: 24px;
  margin-bottom: 4px;
}

.intensity-text {
  line-height: 1.4;
  font-weight: 600;
}

.intensity-btn.active {
  border-color: #2DC78F;
  background: linear-gradient(135deg, #e8f5f0 0%, #f0faf8 100%);
  color: #2DC78F;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.15);
}

/* 营养按钮 */
.nutrition-buttons {
  display: flex;
  gap: 12px;
}

.nutrition-btn {
  flex: 1;
  padding: 14px 8px;
  border: 2px solid #e0e0e0;
  border-radius: 10px;
  font-size: 13px;
  color: #333;
  background-color: #ffffff;
  transition: all 0.3s ease;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 80px;
  font-weight: 600;
}

.nutrition-emoji {
  font-size: 24px;
  margin-bottom: 4px;
  display: block;
}

.nutrition-btn.active {
  border-color: #2DC78F;
  background: linear-gradient(135deg, #e8f5f0 0%, #f0faf8 100%);
  color: #2DC78F;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.15);
}

/* 自定义营养 */
.custom-nutrition {
  background: linear-gradient(135deg, #f9fffe 0%, #f0faf8 100%);
  border-radius: 10px;
  padding: 14px;
  margin-bottom: 16px;
  border-left: 4px solid #2DC78F;
}

/* 结果显示 */
.result-display {
  background: linear-gradient(135deg, #e8f5f0 0%, #f0faf8 100%);
  border-radius: 10px;
  padding: 18px;
  margin-top: 12px;
  border-left: 4px solid #2DC78F;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.1);
}

.result-label {
  font-size: 12px;
  color: #999;
  margin-bottom: 8px;
  font-weight: 600;
}

.result-value {
  font-size: 28px;
  font-weight: 700;
  color: #2DC78F;
}

.result-unit {
  font-size: 14px;
  color: #999;
  margin-left: 4px;
  font-weight: 400;
}

/* 结果占位符 */
.result-placeholder {
  background: linear-gradient(135deg, #f9f9f9 0%, #f5f5f5 100%);
  border-radius: 10px;
  padding: 24px;
  text-align: center;
  margin-top: 12px;
  border: 2px dashed #e0e0e0;
}

.placeholder-text {
  font-size: 13px;
  color: #999;
}

/* 高亮卡片 */
.highlight-card {
  background: linear-gradient(135deg, #2DC78F 0%, #25a870 100%);
  text-align: center;
  padding: 28px 18px;
  box-shadow: 0 8px 24px rgba(45, 199, 143, 0.25);
}

.highlight-label {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.85);
  margin-bottom: 8px;
  font-weight: 600;
}

.highlight-value {
  font-size: 40px;
  font-weight: 700;
  color: #ffffff;
}

.highlight-unit {
  font-size: 16px;
  color: rgba(255, 255, 255, 0.85);
  margin-left: 4px;
  font-weight: 400;
}

/* 营养结果 */
.nutrition-results {
  margin-top: 12px;
  padding-top: 12px;
  border-top: 2px solid #f0f0f0;
}

.nutrition-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 0;
  border-bottom: 1px solid #f9f9f9;
}

.nutrition-item:last-child {
  border-bottom: none;
}

.nutrition-label {
  display: flex;
  align-items: center;
  font-size: 13px;
  color: #666;
  font-weight: 600;
}

.nutrition-emoji {
  font-size: 18px;
  margin-right: 8px;
  display: inline-block;
}

.nutrition-value {
  font-size: 13px;
  color: #2DC78F;
  font-weight: 700;
  text-align: right;
}

/* 第4天部分 */
.day4-section {
  margin-top: 16px;
  padding-top: 16px;
  border-top: 2px dashed #2DC78F;
}

.day4-title {
  font-size: 13px;
  color: #2DC78F;
  font-weight: 700;
  margin-bottom: 12px;
  padding: 8px 12px;
  background-color: rgba(45, 199, 143, 0.08);
  border-radius: 6px;
}

/* 百分比控制 */
.percent-control {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 10px;
}

.percent-btn {
  width: 44px;
  height: 44px;
  border: 2px solid #2DC78F;
  border-radius: 10px;
  font-size: 20px;
  color: #2DC78F;
  background-color: #ffffff;
  font-weight: 700;
  transition: all 0.3s ease;
}

.percent-btn:active {
  background-color: #e8f5f0;
  transform: scale(0.95);
}

.percent-display {
  flex: 1;
  text-align: center;
  font-size: 20px;
  font-weight: 700;
  color: #2DC78F;
  background: linear-gradient(135deg, #e8f5f0 0%, #f0faf8 100%);
  padding: 12px;
  border-radius: 10px;
  border: 2px solid #2DC78F;
}

.percent-hint {
  font-size: 12px;
  color: #999;
  margin-top: 8px;
  text-align: center;
}

/* 结果项 */
.result-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 0;
  border-bottom: 1px solid #f0f0f0;
}

.result-item:last-child {
  border-bottom: none;
}

.result-item .result-label {
  font-size: 13px;
  color: #666;
  margin: 0;
  font-weight: 600;
}

.result-item .result-value {
  font-size: 16px;
  color: #2DC78F;
  font-weight: 700;
  margin: 0;
}

/* 保存按钮 */
.save-btn {
  width: 100%;
  padding: 14px;
  background: linear-gradient(135deg, #2DC78F 0%, #25a870 100%);
  color: #ffffff;
  border-radius: 10px;
  font-size: 15px;
  font-weight: 700;
  margin-top: 18px;
  border: none;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.25);
  transition: all 0.3s ease;
}

.save-btn:active {
  transform: scale(0.98);
  box-shadow: 0 2px 6px rgba(45, 199, 143, 0.15);
}

/* 数据项 */
.data-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 0;
  border-bottom: 1px solid #f0f0f0;
}

.data-item:last-child {
  border-bottom: none;
}

.data-label {
  font-size: 13px;
  color: #666;
  font-weight: 600;
}

.data-value {
  font-size: 14px;
  color: #2DC78F;
  font-weight: 700;
}

/* BMI显示 */
.bmi-display {
  background: linear-gradient(135deg, #e8f5f0 0%, #f0faf8 100%);
  border-radius: 10px;
  padding: 28px;
  text-align: center;
  margin-bottom: 16px;
  border: 2px solid rgba(45, 199, 143, 0.2);
}

.bmi-value {
  font-size: 40px;
  font-weight: 700;
  color: #2DC78F;
}

.bmi-status {
  font-size: 15px;
  color: #2DC78F;
  margin-top: 8px;
  font-weight: 700;
}

/* BMI刻度 */
.bmi-scale {
  display: flex;
  justify-content: space-around;
  margin-bottom: 16px;
  padding: 14px 0;
  border-top: 2px solid #f0f0f0;
  border-bottom: 2px solid #f0f0f0;
}

.scale-item {
  text-align: center;
}

.scale-label {
  font-size: 12px;
  color: #666;
  font-weight: 600;
}

.scale-range {
  font-size: 11px;
  color: #999;
  margin-top: 4px;
}

/* 记录按钮 */
.record-btn {
  width: 100%;
  padding: 14px;
  background: linear-gradient(135deg, #2DC78F 0%, #25a870 100%);
  color: #ffffff;
  border-radius: 10px;
  font-size: 15px;
  font-weight: 700;
  margin-top: 18px;
  border: none;
  box-shadow: 0 4px 12px rgba(45, 199, 143, 0.25);
  transition: all 0.3s ease;
}

.record-btn:active {
  transform: scale(0.98);
  box-shadow: 0 2px 6px rgba(45, 199, 143, 0.15);
}

/* FAQ项 */
.faq-item {
  padding: 14px 0;
  border-bottom: 1px solid #f0f0f0;
}

.faq-item:last-child {
  border-bottom: none;
}

.faq-question {
  font-size: 13px;
  color: #333;
  font-weight: 700;
  margin-bottom: 8px;
}

.faq-answer {
  font-size: 12px;
  color: #999;
  line-height: 1.6;
}
